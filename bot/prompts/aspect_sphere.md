Generate a 1:1 square collectible "aspect sphere" image using the
provided template as layout reference.

The sphere represents the thematic aspect "{aspect_name}".{set_context}
Transform the plain template sphere into a visually striking sphere
that embodies "{aspect_name}".

## Composition & Layout (match the template exactly)

- **Sphere (main focus, ~75% of image):** A large glass sphere filling
  most of the frame. Inside the sphere, depict a vivid, focused
  representation of "{aspect_name}" — this is the centerpiece and
  must be immediately readable and unmistakable.
  - If "{aspect_name}" refers to a specific character, creature, or
    named entity, depict that individual subject prominently.
  - If it refers to a physical object or item, depict a single,
    well-defined instance of it — not multiple copies or a scattered
    collection.
  - If it refers to a setting, mood, or abstract concept, depict a
    representative scene or visual motif.

- **Nameplate (below the sphere, ~15% of image):** A small
  rounded-rectangle label below the sphere, containing the text
  "{aspect_name}" in clean, legible lettering. The nameplate uses
  a {color} color palette to indicate rarity.

- **Background (~10%):** Minimal, dark, or subtle gradient. The sphere
  and nameplate are the sole subjects.

## Sphere Format

- No pedestal, stand, base, legs, or platform under the sphere. The
  sphere floats just above the nameplate area.
- No snow globe appearance — this is a clean, floating glass sphere,
  not a snow globe on a thick mount.

## Rarity Color

The nameplate background and any subtle glow or accent around the
sphere's rim must use a {color} color palette. This color applies
only to the nameplate and subtle accents — not to the sphere interior.
The sphere interior uses whatever colors best represent
"{aspect_name}".

## Thematic Interpretation

The visual content inside the sphere must be a direct, specific
interpretation of "{aspect_name}" — capture the essence of that exact
subject rather than a generic scene loosely associated with it.

Do not default to generic fantasy, magic, or sci-fi imagery unless the
aspect name explicitly calls for it. If "{aspect_name}" is abstract,
ground the visual in a concrete, real-world context.

## Visual Quality

High-quality, detailed digital illustration with rich colors and fine
details visible even at small sizes. The sphere should look like a
premium collectible — polished, luminous, and desirable.

## Output Constraints

- Exactly 1:1 square aspect ratio.
- Sphere centered, filling roughly 75-85% of the image width.
- Nameplate centered below the sphere with "{aspect_name}" text
  clearly readable.
- Minimal background — sphere + nameplate are the sole subjects.

## Creativity Factor: {creativeness_factor}/100

Low (10-20): Clean sphere, simple focused motif inside, plain
nameplate.
Medium (40-60): Detailed depiction inside with thematic surface
effects on the glass.
High (80-100): Spectacular miniature world inside with complex
lighting and extraordinary depth.
