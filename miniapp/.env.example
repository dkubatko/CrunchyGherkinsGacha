# Mini App environment variables
# Copy to .env.production for production builds.
# For local dev (npm run dev), the code defaults to http://localhost:8000.

VITE_API_BASE_URL=https://api.example.com
